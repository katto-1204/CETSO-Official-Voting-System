import * as Lucide from 'lucide-react';
console.log('Github:', Object.keys(Lucide).filter(k => k.toLowerCase().includes('github')));
console.log('Globe:', Object.keys(Lucide).filter(k => k.toLowerCase().includes('globe')));
